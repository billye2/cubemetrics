// Test stub for the `server-only` package, which throws when imported outside a
// React Server Component. Aliased in vitest.config.ts so spine adapters/registry
// (server-only at runtime) can be imported in unit tests.
export {};
